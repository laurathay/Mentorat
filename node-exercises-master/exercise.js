function sum(a, b) {
    return a + b;
}

function hasUniqueLetter(word) {
    // ici implémenter le corps de la fonction pour faire passer les tests
    return true;
}

function isAnagram(wordA, wordB) {
    // ici implémenter le corps de la fonction pour faire passer les tests
    return true;
}

module.exports = {sum, hasUniqueLetter, isAnagram};
  
  